import {
    CORE_STATUS_CAPABILITY,
    createCapabilityToken,
    definePlugin,
    definePluginRef,
} from '@bensitu/image-editor/sdk';

export { CORE_STATUS_CAPABILITY, createCapabilityToken, definePlugin, definePluginRef };

import { ImageEditor } from '@bensitu/image-editor';
import { ImageEditorCore } from '@bensitu/image-editor/core';

const former = new ImageEditor(fabric);
const current = new ImageEditorCore(fabric);
void former;
void current;

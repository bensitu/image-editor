# Vanilla DOM Controls example

This browser example adds the optional DOM Controls Plugin to the Minimal
Preset. Bindings use exact Plugin references, asynchronous actions route errors,
and editor disposal removes every listener.

```bash
npm install
npm run build
npm run dev
```

# Vanilla Core example

This browser example composes `ImageEditorCore`, Transform, and History through
formal public entries. It handles operation failures and disposes the editor on
page teardown.

```bash
npm install
npm run build
npm run dev
```

import type * as FabricNS from 'fabric';
import type { GeometryMutationCoordinator } from '../../core-runtime/geometry/index.js';
import type { CoreExportPort, CoreHostPort, CoreStatePort } from '../../core-runtime/internal-capabilities.js';
import { type Disposable } from '../../plugin-kernel/index.js';
import type { FlattenOptions, OverlayClassification, OverlayExportRenderer, OverlayFoundationApi, OverlayGeometryPolicy, OverlayKindDefinition, OverlayQuery, OverlaySelectionState, OverlaySerializer } from './overlay-types.js';
export declare class OverlayFoundationController implements OverlayFoundationApi, Disposable {
    private readonly host;
    private readonly state;
    private readonly geometry;
    private readonly kinds;
    private readonly policies;
    private readonly serializers;
    private readonly renderers;
    private readonly byId;
    private readonly byObject;
    private readonly selectionListeners;
    private readonly registrations;
    private preservedRecords;
    private registrationSequence;
    private generatedIdSequence;
    private attached;
    private disposed;
    private readonly onObjectAdded;
    private readonly onObjectRemoved;
    private readonly onSelectionChanged;
    constructor(host: CoreHostPort, state: CoreStatePort, geometry: GeometryMutationCoordinator, exportPort: CoreExportPort);
    attach(): void;
    registerKind(definition: OverlayKindDefinition): Disposable;
    registerGeometryPolicy(policy: OverlayGeometryPolicy): Disposable;
    registerSerializer(serializer: OverlaySerializer): Disposable;
    registerExportRenderer(renderer: OverlayExportRenderer): Disposable;
    list(query?: OverlayQuery): readonly FabricNS.FabricObject[];
    getByPersistentId(id: string): FabricNS.FabricObject | null;
    classify(object: FabricNS.FabricObject): OverlayClassification | null;
    getSelection(): OverlaySelectionState;
    select(ids: readonly string[]): void;
    discardSelection(): void;
    onSelectionChange(listener: (state: OverlaySelectionState) => void): Disposable;
    setHidden(id: string, hidden: boolean): void;
    setLocked(id: string, locked: boolean): void;
    bringForward(id: string): void;
    sendBackward(id: string): void;
    bringToFront(id: string): void;
    sendToBack(id: string): void;
    flatten(query?: OverlayQuery, options?: FlattenOptions): Promise<void>;
    dispose(): void;
    private captureState;
    private restoreState;
    private resetState;
    private prepareGeometry;
    private applyGeometry;
    private synchronizeGeometry;
    private rollbackGeometry;
    private renderExport;
    private renderObjects;
    private indexObject;
    private unindexObject;
    private rebuildIndex;
    private classificationFor;
    private indexedCanvasObjects;
    private moveRelative;
    private moveToOverlayIndex;
    private requireIndexed;
    private requireKindOwner;
    private emitSelection;
    private generatePersistentId;
    private createFabricUtilAccess;
    private assertIdentifier;
    private assertActive;
}
